"""
    merged spec

    merged spec

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Optional

from pydantic import BaseModel, Field, StrictBool, StrictStr, conlist, constr, validator

from xero_python.models.validation_error import ValidationError


class Account(BaseModel):
    """
    Account
    """

    code: Optional[StrictStr] = Field(
        default=None,
        alias="Code",
        description="Customer defined alpha numeric account code e.g 200 or SALES (max length = 10)",
    )
    name: Optional[constr(strict=True, max_length=150)] = Field(
        default=None, alias="Name", description="Name of account (max length = 150)"
    )
    account_id: Optional[StrictStr] = Field(
        default=None,
        alias="AccountID",
        description="The Xero identifier for an account – specified as a string following  the endpoint name   e.g. /297c2dc5-cc47-4afd-8ec8-74990b8761e9",
    )
    type: Optional[StrictStr] = Field(default=None, alias="Type")
    bank_account_number: Optional[StrictStr] = Field(
        default=None, alias="BankAccountNumber", description="For bank accounts only (Account Type BANK)"
    )
    status: Optional[StrictStr] = Field(
        default=None,
        alias="Status",
        description="Accounts with a status of ACTIVE can be updated to ARCHIVED. See Account Status Codes",
    )
    description: Optional[StrictStr] = Field(
        default=None,
        alias="Description",
        description="Description of the Account. Valid for all types of accounts except bank accounts (max length = 4000)",
    )
    bank_account_type: Optional[StrictStr] = Field(
        default=None, alias="BankAccountType", description="For bank accounts only. See Bank Account types"
    )
    currency_code: Optional[StrictStr] = Field(default=None, alias="CurrencyCode")
    tax_type: Optional[StrictStr] = Field(default=None, alias="TaxType", description="The tax type from taxRates")
    enable_payments_to_account: Optional[StrictBool] = Field(
        default=None,
        alias="EnablePaymentsToAccount",
        description="Boolean – describes whether account can have payments applied to it",
    )
    show_in_expense_claims: Optional[StrictBool] = Field(
        default=None,
        alias="ShowInExpenseClaims",
        description="Boolean – describes whether account code is available for use with expense claims",
    )
    var_class: Optional[StrictStr] = Field(default=None, alias="Class", description="See Account Class Types")
    system_account: Optional[StrictStr] = Field(
        default=None,
        alias="SystemAccount",
        description="If this is a system account then this element is returned. See System Account types. Note that non-system accounts may have this element set as either “” or null.",
    )
    reporting_code: Optional[StrictStr] = Field(default=None, alias="ReportingCode", description="Shown if set")
    reporting_code_name: Optional[StrictStr] = Field(
        default=None, alias="ReportingCodeName", description="Shown if set"
    )
    has_attachments: Optional[StrictBool] = Field(
        default=False,
        alias="HasAttachments",
        description="boolean to indicate if an account has an attachment (read only)",
    )
    updated_date_utc: Optional[StrictStr] = Field(
        default=None, alias="UpdatedDateUTC", description="Last modified date UTC format"
    )
    add_to_watchlist: Optional[StrictBool] = Field(
        default=None,
        alias="AddToWatchlist",
        description="Boolean – describes whether the account is shown in the watchlist widget on the dashboard",
    )
    validation_errors: Optional[conlist(ValidationError)] = Field(
        default=None, alias="ValidationErrors", description="Displays array of validation error messages from the API"
    )
    __properties = [
        "Code",
        "Name",
        "AccountID",
        "Type",
        "BankAccountNumber",
        "Status",
        "Description",
        "BankAccountType",
        "CurrencyCode",
        "TaxType",
        "EnablePaymentsToAccount",
        "ShowInExpenseClaims",
        "Class",
        "SystemAccount",
        "ReportingCode",
        "ReportingCodeName",
        "HasAttachments",
        "UpdatedDateUTC",
        "AddToWatchlist",
        "ValidationErrors",
    ]

    @validator("status")
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ("ACTIVE", "ARCHIVED", "DELETED"):
            raise ValueError("must be one of enum values ('ACTIVE', 'ARCHIVED', 'DELETED')")
        return value

    @validator("bank_account_type")
    def bank_account_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ("BANK", "CREDITCARD", "PAYPAL", "NONE", ""):
            raise ValueError("must be one of enum values ('BANK', 'CREDITCARD', 'PAYPAL', 'NONE', '')")
        return value

    @validator("var_class")
    def var_class_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ("ASSET", "EQUITY", "EXPENSE", "LIABILITY", "REVENUE"):
            raise ValueError("must be one of enum values ('ASSET', 'EQUITY', 'EXPENSE', 'LIABILITY', 'REVENUE')")
        return value

    @validator("system_account")
    def system_account_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in (
            "DEBTORS",
            "CREDITORS",
            "BANKCURRENCYGAIN",
            "GST",
            "GSTONIMPORTS",
            "HISTORICAL",
            "REALISEDCURRENCYGAIN",
            "RETAINEDEARNINGS",
            "ROUNDING",
            "TRACKINGTRANSFERS",
            "UNPAIDEXPCLM",
            "UNREALISEDCURRENCYGAIN",
            "WAGEPAYABLES",
            "CISASSETS",
            "CISASSET",
            "CISLABOUR",
            "CISLABOUREXPENSE",
            "CISLABOURINCOME",
            "CISLIABILITY",
            "CISMATERIALS",
            "",
        ):
            raise ValueError(
                "must be one of enum values ('DEBTORS', 'CREDITORS', 'BANKCURRENCYGAIN', 'GST', 'GSTONIMPORTS', 'HISTORICAL', 'REALISEDCURRENCYGAIN', 'RETAINEDEARNINGS', 'ROUNDING', 'TRACKINGTRANSFERS', 'UNPAIDEXPCLM', 'UNREALISEDCURRENCYGAIN', 'WAGEPAYABLES', 'CISASSETS', 'CISASSET', 'CISLABOUR', 'CISLABOUREXPENSE', 'CISLABOURINCOME', 'CISLIABILITY', 'CISMATERIALS', '')"
            )
        return value

    class Config:
        """Pydantic configuration"""

        allow_population_by_field_name = True
        validate_assignment = True

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Account:
        """Create an instance of Account from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self):
        """Returns the dictionary representation of the model using alias"""
        _dict = self.dict(
            by_alias=True,
            exclude={"var_class", "system_account", "reporting_code_name", "has_attachments", "updated_date_utc"},
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in validation_errors (list)
        _items = []
        if self.validation_errors:
            for _item in self.validation_errors:
                if _item:
                    _items.append(_item.to_dict())
            _dict["ValidationErrors"] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: dict) -> Account:
        """Create an instance of Account from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return Account.parse_obj(obj)

        _obj = Account.parse_obj(
            {
                "code": obj.get("Code"),
                "name": obj.get("Name"),
                "account_id": obj.get("AccountID"),
                "type": obj.get("Type"),
                "bank_account_number": obj.get("BankAccountNumber"),
                "status": obj.get("Status"),
                "description": obj.get("Description"),
                "bank_account_type": obj.get("BankAccountType"),
                "currency_code": obj.get("CurrencyCode"),
                "tax_type": obj.get("TaxType"),
                "enable_payments_to_account": obj.get("EnablePaymentsToAccount"),
                "show_in_expense_claims": obj.get("ShowInExpenseClaims"),
                "var_class": obj.get("Class"),
                "system_account": obj.get("SystemAccount"),
                "reporting_code": obj.get("ReportingCode"),
                "reporting_code_name": obj.get("ReportingCodeName"),
                "has_attachments": obj.get("HasAttachments") if obj.get("HasAttachments") is not None else False,
                "updated_date_utc": obj.get("UpdatedDateUTC"),
                "add_to_watchlist": obj.get("AddToWatchlist"),
                "validation_errors": [ValidationError.from_dict(_item) for _item in obj.get("ValidationErrors")]
                if obj.get("ValidationErrors") is not None
                else None,
            }
        )
        return _obj
