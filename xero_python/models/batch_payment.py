"""
    merged spec

    merged spec

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Optional, Union

from pydantic import BaseModel, Field, StrictBool, StrictFloat, StrictInt, StrictStr, conlist, constr, validator

from xero_python.models.account import Account
from xero_python.models.validation_error import ValidationError


class BatchPayment(BaseModel):
    """
    BatchPayment
    """

    account: Optional[Account] = Field(default=None, alias="Account")
    reference: Optional[constr(strict=True, max_length=255)] = Field(
        default=None,
        alias="Reference",
        description="(NZ Only) Optional references for the batch payment transaction. It will also show with the batch payment transaction in the bank reconciliation Find & Match screen. Depending on your individual bank, the detail may also show on the bank statement you import into Xero.",
    )
    particulars: Optional[constr(strict=True, max_length=12)] = Field(
        default=None,
        alias="Particulars",
        description="(NZ Only) Optional references for the batch payment transaction. It will also show with the batch payment transaction in the bank reconciliation Find & Match screen. Depending on your individual bank, the detail may also show on the bank statement you import into Xero.",
    )
    code: Optional[constr(strict=True, max_length=12)] = Field(
        default=None,
        alias="Code",
        description="(NZ Only) Optional references for the batch payment transaction. It will also show with the batch payment transaction in the bank reconciliation Find & Match screen. Depending on your individual bank, the detail may also show on the bank statement you import into Xero.",
    )
    details: Optional[StrictStr] = Field(
        default=None,
        alias="Details",
        description="(Non-NZ Only) These details are sent to the org’s bank as a reference for the batch payment transaction. They will also show with the batch payment transaction in the bank reconciliation Find & Match screen. Depending on your individual bank, the detail may also show on the bank statement imported into Xero. Maximum field length = 18",
    )
    narrative: Optional[constr(strict=True, max_length=18)] = Field(
        default=None,
        alias="Narrative",
        description="(UK Only) Only shows on the statement line in Xero. Max length =18",
    )
    batch_payment_id: Optional[StrictStr] = Field(
        default=None,
        alias="BatchPaymentID",
        description="The Xero generated unique identifier for the bank transaction (read-only)",
    )
    date_string: Optional[StrictStr] = Field(
        default=None, alias="DateString", description="Date the payment is being made (YYYY-MM-DD) e.g. 2009-09-06"
    )
    var_date: Optional[StrictStr] = Field(
        default=None, alias="Date", description="Date the payment is being made (YYYY-MM-DD) e.g. 2009-09-06"
    )
    amount: Optional[Union[StrictFloat, StrictInt]] = Field(
        default=None,
        alias="Amount",
        description="The amount of the payment. Must be less than or equal to the outstanding amount owing on the invoice e.g. 200.00",
    )
    payments: Optional[conlist(Payment)] = Field(default=None, alias="Payments", description="An array of payments")
    type: Optional[StrictStr] = Field(
        default=None,
        alias="Type",
        description="PAYBATCH for bill payments or RECBATCH for sales invoice payments (read-only)",
    )
    status: Optional[StrictStr] = Field(
        default=None,
        alias="Status",
        description="AUTHORISED or DELETED (read-only). New batch payments will have a status of AUTHORISED. It is not possible to delete batch payments via the API.",
    )
    total_amount: Optional[Union[StrictFloat, StrictInt]] = Field(
        default=None, alias="TotalAmount", description="The total of the payments that make up the batch (read-only)"
    )
    updated_date_utc: Optional[StrictStr] = Field(
        default=None, alias="UpdatedDateUTC", description="UTC timestamp of last update to the payment"
    )
    is_reconciled: Optional[StrictBool] = Field(
        default=None,
        alias="IsReconciled",
        description="Booelan that tells you if the batch payment has been reconciled (read-only)",
    )
    validation_errors: Optional[conlist(ValidationError)] = Field(
        default=None, alias="ValidationErrors", description="Displays array of validation error messages from the API"
    )
    __properties = [
        "Account",
        "Reference",
        "Particulars",
        "Code",
        "Details",
        "Narrative",
        "BatchPaymentID",
        "DateString",
        "Date",
        "Amount",
        "Payments",
        "Type",
        "Status",
        "TotalAmount",
        "UpdatedDateUTC",
        "IsReconciled",
        "ValidationErrors",
    ]

    @validator("type")
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ("PAYBATCH", "RECBATCH"):
            raise ValueError("must be one of enum values ('PAYBATCH', 'RECBATCH')")
        return value

    @validator("status")
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ("AUTHORISED", "DELETED"):
            raise ValueError("must be one of enum values ('AUTHORISED', 'DELETED')")
        return value

    class Config:
        """Pydantic configuration"""

        allow_population_by_field_name = True
        validate_assignment = True

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> BatchPayment:
        """Create an instance of BatchPayment from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self):
        """Returns the dictionary representation of the model using alias"""
        _dict = self.dict(
            by_alias=True,
            exclude={"batch_payment_id", "type", "status", "total_amount", "updated_date_utc", "is_reconciled"},
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of account
        if self.account:
            _dict["Account"] = self.account.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in payments (list)
        _items = []
        if self.payments:
            for _item in self.payments:
                if _item:
                    _items.append(_item.to_dict())
            _dict["Payments"] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in validation_errors (list)
        _items = []
        if self.validation_errors:
            for _item in self.validation_errors:
                if _item:
                    _items.append(_item.to_dict())
            _dict["ValidationErrors"] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: dict) -> BatchPayment:
        """Create an instance of BatchPayment from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return BatchPayment.parse_obj(obj)

        _obj = BatchPayment.parse_obj(
            {
                "account": Account.from_dict(obj.get("Account")) if obj.get("Account") is not None else None,
                "reference": obj.get("Reference"),
                "particulars": obj.get("Particulars"),
                "code": obj.get("Code"),
                "details": obj.get("Details"),
                "narrative": obj.get("Narrative"),
                "batch_payment_id": obj.get("BatchPaymentID"),
                "date_string": obj.get("DateString"),
                "var_date": obj.get("Date"),
                "amount": obj.get("Amount"),
                "payments": [Payment.from_dict(_item) for _item in obj.get("Payments")]
                if obj.get("Payments") is not None
                else None,
                "type": obj.get("Type"),
                "status": obj.get("Status"),
                "total_amount": obj.get("TotalAmount"),
                "updated_date_utc": obj.get("UpdatedDateUTC"),
                "is_reconciled": obj.get("IsReconciled"),
                "validation_errors": [ValidationError.from_dict(_item) for _item in obj.get("ValidationErrors")]
                if obj.get("ValidationErrors") is not None
                else None,
            }
        )
        return _obj


from xero_python.models.payment import Payment

BatchPayment.update_forward_refs()
