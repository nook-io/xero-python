"""
    merged spec

    merged spec

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations

import json
import pprint
import re  # noqa: F401

from pydantic import BaseModel, Field, StrictStr, conlist, validator

from xero_python.models.subscription_item import SubscriptionItem


class Plan(BaseModel):
    """
    Plan
    """

    id: StrictStr = Field(default=..., description="The unique identifier of the plan")
    name: StrictStr = Field(
        default=..., description="The name of the plan. It is used in the invoice line item description. "
    )
    status: StrictStr = Field(
        default=..., description="Status of the plan. Available statuses are ACTIVE, CANCELED, and PENDING_ACTIVATION. "
    )
    subscription_items: conlist(SubscriptionItem) = Field(
        default=...,
        alias="subscriptionItems",
        description="List of the subscription items belonging to the plan. It does not include cancelled subscription items. ",
    )
    __properties = ["id", "name", "status", "subscriptionItems"]

    @validator("status")
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in ("ACTIVE", "CANCELED", "PENDING_ACTIVATION"):
            raise ValueError("must be one of enum values ('ACTIVE', 'CANCELED', 'PENDING_ACTIVATION')")
        return value

    class Config:
        """Pydantic configuration"""

        allow_population_by_field_name = True
        validate_assignment = True

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Plan:
        """Create an instance of Plan from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self):
        """Returns the dictionary representation of the model using alias"""
        _dict = self.dict(by_alias=True, exclude={}, exclude_none=True)
        # override the default output from pydantic by calling `to_dict()` of each item in subscription_items (list)
        _items = []
        if self.subscription_items:
            for _item in self.subscription_items:
                if _item:
                    _items.append(_item.to_dict())
            _dict["subscriptionItems"] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: dict) -> Plan:
        """Create an instance of Plan from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return Plan.parse_obj(obj)

        _obj = Plan.parse_obj(
            {
                "id": obj.get("id"),
                "name": obj.get("name"),
                "status": obj.get("status"),
                "subscription_items": [SubscriptionItem.from_dict(_item) for _item in obj.get("subscriptionItems")]
                if obj.get("subscriptionItems") is not None
                else None,
            }
        )
        return _obj
