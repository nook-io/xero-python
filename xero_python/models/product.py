"""
    merged spec

    merged spec

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Optional

from pydantic import BaseModel, Field, StrictStr, validator


class Product(BaseModel):
    """
    Product
    """

    id: Optional[StrictStr] = Field(default=None, description="The unique identifier for the product")
    name: Optional[StrictStr] = Field(default=None, description="The name of the product")
    seat_unit: Optional[StrictStr] = Field(
        default=None,
        alias="seatUnit",
        description='The unit of the per seat product. e.g. "user", "organisation", "SMS", etc',
    )
    type: Optional[StrictStr] = Field(
        default=None,
        description="The pricing model of the product: * FIXED: Customers are charged a fixed amount for each billing period * PER_SEAT: Customers are charged based on the number of units they purchase * METERED: Customers are charged per use of this product ",
    )
    usage_unit: Optional[StrictStr] = Field(
        default=None, alias="usageUnit", description='The unit of the usage product. e.g. "user", "minutes", "SMS", etc'
    )
    __properties = ["id", "name", "seatUnit", "type", "usageUnit"]

    @validator("type")
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ("FIXED", "PER_SEAT", "METERED"):
            raise ValueError("must be one of enum values ('FIXED', 'PER_SEAT', 'METERED')")
        return value

    class Config:
        """Pydantic configuration"""

        allow_population_by_field_name = True
        validate_assignment = True

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Product:
        """Create an instance of Product from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self):
        """Returns the dictionary representation of the model using alias"""
        _dict = self.dict(by_alias=True, exclude={}, exclude_none=True)
        return _dict

    @classmethod
    def from_dict(cls, obj: dict) -> Product:
        """Create an instance of Product from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return Product.parse_obj(obj)

        _obj = Product.parse_obj(
            {
                "id": obj.get("id"),
                "name": obj.get("name"),
                "seat_unit": obj.get("seatUnit"),
                "type": obj.get("type"),
                "usage_unit": obj.get("usageUnit"),
            }
        )
        return _obj
