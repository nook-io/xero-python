"""
    merged spec

    merged spec

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Optional

from pydantic import BaseModel, Field, StrictInt, StrictStr, validator


class Schedule(BaseModel):
    """
    Schedule
    """

    period: Optional[StrictInt] = Field(
        default=None, alias="Period", description="Integer used with the unit e.g. 1 (every 1 week), 2 (every 2 months)"
    )
    unit: Optional[StrictStr] = Field(
        default=None, alias="Unit", description="One of the following - WEEKLY or MONTHLY"
    )
    due_date: Optional[StrictInt] = Field(
        default=None,
        alias="DueDate",
        description="Integer used with due date type e.g 20 (of following month), 31 (of current month)",
    )
    due_date_type: Optional[StrictStr] = Field(default=None, alias="DueDateType", description="the payment terms")
    start_date: Optional[StrictStr] = Field(
        default=None,
        alias="StartDate",
        description="Date the first invoice of the current version of the repeating schedule was generated (changes when repeating invoice is edited)",
    )
    next_scheduled_date: Optional[StrictStr] = Field(
        default=None,
        alias="NextScheduledDate",
        description="The calendar date of the next invoice in the schedule to be generated",
    )
    end_date: Optional[StrictStr] = Field(
        default=None,
        alias="EndDate",
        description="Invoice end date – only returned if the template has an end date set",
    )
    __properties = ["Period", "Unit", "DueDate", "DueDateType", "StartDate", "NextScheduledDate", "EndDate"]

    @validator("unit")
    def unit_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ("WEEKLY", "MONTHLY"):
            raise ValueError("must be one of enum values ('WEEKLY', 'MONTHLY')")
        return value

    @validator("due_date_type")
    def due_date_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in (
            "DAYSAFTERBILLDATE",
            "DAYSAFTERBILLMONTH",
            "DAYSAFTERINVOICEDATE",
            "DAYSAFTERINVOICEMONTH",
            "OFCURRENTMONTH",
            "OFFOLLOWINGMONTH",
        ):
            raise ValueError(
                "must be one of enum values ('DAYSAFTERBILLDATE', 'DAYSAFTERBILLMONTH', 'DAYSAFTERINVOICEDATE', 'DAYSAFTERINVOICEMONTH', 'OFCURRENTMONTH', 'OFFOLLOWINGMONTH')"
            )
        return value

    class Config:
        """Pydantic configuration"""

        allow_population_by_field_name = True
        validate_assignment = True

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Schedule:
        """Create an instance of Schedule from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self):
        """Returns the dictionary representation of the model using alias"""
        _dict = self.dict(by_alias=True, exclude={}, exclude_none=True)
        return _dict

    @classmethod
    def from_dict(cls, obj: dict) -> Schedule:
        """Create an instance of Schedule from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return Schedule.parse_obj(obj)

        _obj = Schedule.parse_obj(
            {
                "period": obj.get("Period"),
                "unit": obj.get("Unit"),
                "due_date": obj.get("DueDate"),
                "due_date_type": obj.get("DueDateType"),
                "start_date": obj.get("StartDate"),
                "next_scheduled_date": obj.get("NextScheduledDate"),
                "end_date": obj.get("EndDate"),
            }
        )
        return _obj
