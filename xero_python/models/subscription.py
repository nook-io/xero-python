"""
    merged spec

    merged spec

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from datetime import datetime
from typing import Optional

from pydantic import BaseModel, Field, StrictBool, StrictStr, conlist, validator

from xero_python.models.plan import Plan


class Subscription(BaseModel):
    """
    Subscription
    """

    current_period_end: datetime = Field(
        default=...,
        alias="currentPeriodEnd",
        description="End of the current period that the subscription has been invoiced for. ",
    )
    end_date: Optional[datetime] = Field(
        default=None,
        alias="endDate",
        description="If the subscription has been canceled, this is the date when the subscription ends. If null, the subscription is active and has not been cancelled",
    )
    id: StrictStr = Field(default=..., description="The unique identifier of the subscription")
    organisation_id: StrictStr = Field(
        default=..., alias="organisationId", description="The Xero generated unique identifier for the organisation"
    )
    plans: conlist(Plan) = Field(default=..., description="List of plans for the subscription.")
    start_date: datetime = Field(
        default=..., alias="startDate", description="Date when the subscription was first created."
    )
    status: StrictStr = Field(
        default=..., description="Status of the subscription. Available statuses are ACTIVE, CANCELED, and PAST_DUE."
    )
    test_mode: Optional[StrictBool] = Field(
        default=None, alias="testMode", description="Boolean used to indicate if the subscription is in test mode"
    )
    __properties = ["currentPeriodEnd", "endDate", "id", "organisationId", "plans", "startDate", "status", "testMode"]

    @validator("status")
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in ("ACTIVE", "CANCELED", "PAST_DUE"):
            raise ValueError("must be one of enum values ('ACTIVE', 'CANCELED', 'PAST_DUE')")
        return value

    class Config:
        """Pydantic configuration"""

        allow_population_by_field_name = True
        validate_assignment = True

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Subscription:
        """Create an instance of Subscription from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self):
        """Returns the dictionary representation of the model using alias"""
        _dict = self.dict(by_alias=True, exclude={}, exclude_none=True)
        # override the default output from pydantic by calling `to_dict()` of each item in plans (list)
        _items = []
        if self.plans:
            for _item in self.plans:
                if _item:
                    _items.append(_item.to_dict())
            _dict["plans"] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: dict) -> Subscription:
        """Create an instance of Subscription from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return Subscription.parse_obj(obj)

        _obj = Subscription.parse_obj(
            {
                "current_period_end": obj.get("currentPeriodEnd"),
                "end_date": obj.get("endDate"),
                "id": obj.get("id"),
                "organisation_id": obj.get("organisationId"),
                "plans": [Plan.from_dict(_item) for _item in obj.get("plans")]
                if obj.get("plans") is not None
                else None,
                "start_date": obj.get("startDate"),
                "status": obj.get("status"),
                "test_mode": obj.get("testMode"),
            }
        )
        return _obj
