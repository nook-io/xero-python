"""
    merged spec

    merged spec

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from datetime import datetime
from typing import Optional

from pydantic import BaseModel, Field, StrictBool, StrictInt, StrictStr, validator

from xero_python.models.price import Price
from xero_python.models.product import Product


class SubscriptionItem(BaseModel):
    """
    SubscriptionItem
    """

    end_date: Optional[datetime] = Field(
        default=None, alias="endDate", description="Date when the subscription to this product will end"
    )
    id: StrictStr = Field(default=..., description="The unique identifier of the subscription item.")
    price: Price = Field(...)
    product: Product = Field(...)
    quantity: Optional[StrictInt] = Field(
        default=None,
        description="The quantity of the item. For a fixed product, it is 1. For a per-seat product, it is a positive integer. For metered products, it is always null.",
    )
    start_date: datetime = Field(
        default=...,
        alias="startDate",
        description="Date the subscription started, or will start. Note: this could be in the future for downgrades or reduced number of seats that haven't taken effect yet. ",
    )
    status: StrictStr = Field(
        default=...,
        description="Status of the subscription item. Available statuses are ACTIVE, CANCELED, and PENDING_ACTIVATION. ",
    )
    test_mode: Optional[StrictBool] = Field(
        default=None, alias="testMode", description="If the subscription is a test subscription"
    )
    __properties = ["endDate", "id", "price", "product", "quantity", "startDate", "status", "testMode"]

    @validator("status")
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in ("ACTIVE", "CANCELED", "PENDING_ACTIVATION"):
            raise ValueError("must be one of enum values ('ACTIVE', 'CANCELED', 'PENDING_ACTIVATION')")
        return value

    class Config:
        """Pydantic configuration"""

        allow_population_by_field_name = True
        validate_assignment = True

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> SubscriptionItem:
        """Create an instance of SubscriptionItem from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self):
        """Returns the dictionary representation of the model using alias"""
        _dict = self.dict(by_alias=True, exclude={}, exclude_none=True)
        # override the default output from pydantic by calling `to_dict()` of price
        if self.price:
            _dict["price"] = self.price.to_dict()
        # override the default output from pydantic by calling `to_dict()` of product
        if self.product:
            _dict["product"] = self.product.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: dict) -> SubscriptionItem:
        """Create an instance of SubscriptionItem from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return SubscriptionItem.parse_obj(obj)

        _obj = SubscriptionItem.parse_obj(
            {
                "end_date": obj.get("endDate"),
                "id": obj.get("id"),
                "price": Price.from_dict(obj.get("price")) if obj.get("price") is not None else None,
                "product": Product.from_dict(obj.get("product")) if obj.get("product") is not None else None,
                "quantity": obj.get("quantity"),
                "start_date": obj.get("startDate"),
                "status": obj.get("status"),
                "test_mode": obj.get("testMode"),
            }
        )
        return _obj
