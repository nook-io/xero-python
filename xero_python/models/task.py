"""
merged spec

merged spec

The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Optional, Union

from pydantic import BaseModel, Field, StrictFloat, StrictInt, StrictStr, validator

from xero_python.models.amount import Amount


class Task(BaseModel):
    """
    Task
    """

    task_id: Optional[StrictStr] = Field(default=None, alias="taskId", description="Identifier of the task.")
    name: Optional[StrictStr] = Field(default=None, description="Name of the task.")
    rate: Optional[Union[StrictFloat, StrictInt]] = None
    charge_type: Optional[StrictStr] = Field(default=None, alias="chargeType")
    estimate_minutes: Optional[StrictInt] = Field(
        default=None, alias="estimateMinutes", description="An estimated time to perform the task"
    )
    project_id: Optional[StrictStr] = Field(
        default=None, alias="projectId", description="Identifier of the project task belongs to."
    )
    total_minutes: Optional[StrictInt] = Field(
        default=None,
        alias="totalMinutes",
        description="Total minutes which have been logged against the task. Logged by assigning a time entry to a task",
    )
    total_amount: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, alias="totalAmount")
    minutes_invoiced: Optional[StrictInt] = Field(
        default=None, alias="minutesInvoiced", description="Minutes on this task which have been invoiced."
    )
    minutes_to_be_invoiced: Optional[StrictInt] = Field(
        default=None, alias="minutesToBeInvoiced", description="Minutes on this task which have not been invoiced."
    )
    fixed_minutes: Optional[StrictInt] = Field(
        default=None,
        alias="fixedMinutes",
        description="Minutes logged against this task if its charge type is `FIXED`.",
    )
    non_chargeable_minutes: Optional[StrictInt] = Field(
        default=None,
        alias="nonChargeableMinutes",
        description="Minutes logged against this task if its charge type is `NON_CHARGEABLE`.",
    )
    amount_to_be_invoiced: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, alias="amountToBeInvoiced")
    amount_invoiced: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, alias="amountInvoiced")
    status: Optional[StrictStr] = Field(
        default=None,
        description="Status of the task. When a task of ChargeType is `FIXED` and the rate amount is invoiced the status will be set to `INVOICED` and can't be modified. A task with ChargeType of `TIME` or `NON_CHARGEABLE` cannot have a status of `INVOICED`. A `LOCKED` state indicates that the task is currently changing state (for example being invoiced) and can't be modified.",
    )
    __properties = [
        "taskId",
        "name",
        "rate",
        "chargeType",
        "estimateMinutes",
        "projectId",
        "totalMinutes",
        "totalAmount",
        "minutesInvoiced",
        "minutesToBeInvoiced",
        "fixedMinutes",
        "nonChargeableMinutes",
        "amountToBeInvoiced",
        "amountInvoiced",
        "status",
    ]

    @validator("status")
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ("ACTIVE", "INVOICED", "LOCKED"):
            raise ValueError("must be one of enum values ('ACTIVE', 'INVOICED', 'LOCKED')")
        return value

    class Config:
        """Pydantic configuration"""

        allow_population_by_field_name = True
        validate_assignment = True

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Task:
        """Create an instance of Task from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self):
        """Returns the dictionary representation of the model using alias"""
        _dict = self.dict(by_alias=True, exclude={}, exclude_none=True)
        # override the default output from pydantic by calling `to_dict()` of rate
        if self.rate:
            _dict["rate"] = self.rate.to_dict()
        # override the default output from pydantic by calling `to_dict()` of total_amount
        if self.total_amount:
            _dict["totalAmount"] = self.total_amount.to_dict()
        # override the default output from pydantic by calling `to_dict()` of amount_to_be_invoiced
        if self.amount_to_be_invoiced:
            _dict["amountToBeInvoiced"] = self.amount_to_be_invoiced.to_dict()
        # override the default output from pydantic by calling `to_dict()` of amount_invoiced
        if self.amount_invoiced:
            _dict["amountInvoiced"] = self.amount_invoiced.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: dict) -> Task:
        """Create an instance of Task from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return Task.parse_obj(obj)

        _obj = Task.parse_obj(
            {
                "task_id": obj.get("taskId"),
                "name": obj.get("name"),
                "rate": Amount.from_dict(obj.get("rate")) if obj.get("rate") is not None else None,
                "charge_type": obj.get("chargeType"),
                "estimate_minutes": obj.get("estimateMinutes"),
                "project_id": obj.get("projectId"),
                "total_minutes": obj.get("totalMinutes"),
                "total_amount": Amount.from_dict(obj.get("totalAmount"))
                if obj.get("totalAmount") is not None
                else None,
                "minutes_invoiced": obj.get("minutesInvoiced"),
                "minutes_to_be_invoiced": obj.get("minutesToBeInvoiced"),
                "fixed_minutes": obj.get("fixedMinutes"),
                "non_chargeable_minutes": obj.get("nonChargeableMinutes"),
                "amount_to_be_invoiced": Amount.from_dict(obj.get("amountToBeInvoiced"))
                if obj.get("amountToBeInvoiced") is not None
                else None,
                "amount_invoiced": Amount.from_dict(obj.get("amountInvoiced"))
                if obj.get("amountInvoiced") is not None
                else None,
                "status": obj.get("status"),
            }
        )
        return _obj
