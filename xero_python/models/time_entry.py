"""
    merged spec

    merged spec

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from datetime import datetime
from typing import Optional

from pydantic import BaseModel, Field, StrictInt, StrictStr, validator


class TimeEntry(BaseModel):
    """
    TimeEntry
    """

    time_entry_id: Optional[StrictStr] = Field(
        default=None, alias="timeEntryId", description="Identifier of the time entry."
    )
    user_id: Optional[StrictStr] = Field(
        default=None, alias="userId", description="The xero user identifier of the person who logged time."
    )
    project_id: Optional[StrictStr] = Field(
        default=None,
        alias="projectId",
        description="Identifier of the project, that the task (which the time entry is logged against) belongs to.",
    )
    task_id: Optional[StrictStr] = Field(
        default=None, alias="taskId", description="Identifier of the task that time entry is logged against."
    )
    date_utc: Optional[datetime] = Field(
        default=None,
        alias="dateUtc",
        description="The date time that time entry is logged on. UTC Date Time in ISO-8601 format.",
    )
    date_entered_utc: Optional[datetime] = Field(
        default=None,
        alias="dateEnteredUtc",
        description="The date time that time entry is created. UTC Date Time in ISO-8601 format. By default it is set to server time.",
    )
    duration: Optional[StrictInt] = Field(default=None, description="The duration of logged minutes.")
    description: Optional[StrictStr] = Field(default=None, description="A description of the time entry.")
    status: Optional[StrictStr] = Field(
        default=None,
        description="Status of the time entry. By default a time entry is created with status of `ACTIVE`. A `LOCKED` state indicates that the time entry is currently changing state (for example being invoiced). Updates are not allowed when in this state. It will have a status of INVOICED once it is invoiced.",
    )
    __properties = [
        "timeEntryId",
        "userId",
        "projectId",
        "taskId",
        "dateUtc",
        "dateEnteredUtc",
        "duration",
        "description",
        "status",
    ]

    @validator("status")
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ("ACTIVE", "LOCKED", "INVOICED"):
            raise ValueError("must be one of enum values ('ACTIVE', 'LOCKED', 'INVOICED')")
        return value

    class Config:
        """Pydantic configuration"""

        allow_population_by_field_name = True
        validate_assignment = True

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> TimeEntry:
        """Create an instance of TimeEntry from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self):
        """Returns the dictionary representation of the model using alias"""
        _dict = self.dict(by_alias=True, exclude={}, exclude_none=True)
        return _dict

    @classmethod
    def from_dict(cls, obj: dict) -> TimeEntry:
        """Create an instance of TimeEntry from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return TimeEntry.parse_obj(obj)

        _obj = TimeEntry.parse_obj(
            {
                "time_entry_id": obj.get("timeEntryId"),
                "user_id": obj.get("userId"),
                "project_id": obj.get("projectId"),
                "task_id": obj.get("taskId"),
                "date_utc": obj.get("dateUtc"),
                "date_entered_utc": obj.get("dateEnteredUtc"),
                "duration": obj.get("duration"),
                "description": obj.get("description"),
                "status": obj.get("status"),
            }
        )
        return _obj
